# Runtime mode: demo, onboarding, or personal
APP_MODE=demo

# This project intentionally uses 8766 so it can run beside an existing dashboard.
HOST=127.0.0.1
PORT=8766

# Optional. The resolved path must stay inside this repository's data directory.
# JOB_SEARCH_DB_PATH=data/job_search_operations_dev.sqlite
